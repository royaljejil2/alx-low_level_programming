#include <stdio.h>
/**
 * main - entry point
 * @argv: An array containing the program command
 * line arguments
 *
 * @argc: The number of commands in line arguments
 * Return: Always (0))
 */

int main(int argc __attribute__((unused)), char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
